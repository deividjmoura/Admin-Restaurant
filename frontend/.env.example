# Same-origin deployment: proxy /api to the backend, preserving Host.
VITE_BASE_DOMAIN=localhost
VITE_API_URL=
# Optional only for an unknown dev transport host; never used on apex/platform.
# VITE_DEV_TENANT_SLUG=
# VITE_PROXY_TARGET=http://127.0.0.1:3000

# Produção cross-origin (SPA ≠ API):
# VITE_API_URL=https://admin-restaurant-api.onrender.com
# VITE_TENANT_SLUG=demo
