#!/usr/bin/env node
/**
 * Seed inicial — será expandido na Fase 1 (stores + usuários).
 */
import 'dotenv/config';

console.log('Seed ainda não implementado. Ver issues da Fase 1.');
process.exit(0);
